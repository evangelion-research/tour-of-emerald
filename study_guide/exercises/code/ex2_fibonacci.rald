# Exercise 2.2 — n-th Fibonacci number.
def fib(n: int) -> int {
    a: int = 0
    b: int = 1
    for i in range(n) {
        next: int = a + b
        a = b
        b = next
    }
    return a
}

print(fib(10))
