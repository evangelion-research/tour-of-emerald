# Exercise 2.1 — factorial.
def factorial(n: int) -> int {
    result: int = 1
    for i in range(1, n + 1) {
        result = result * i
    }
    return result
}

print(factorial(0))
print(factorial(5))
print(factorial(10))
